[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# Serve HTML files directly (no redirect)
# This ensures dashboard.html, callback.html, etc. are accessible

[build.environment]
  NODE_VERSION = "18"

